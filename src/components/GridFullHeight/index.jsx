import React from 'react';
import useStyles from './styles'

const GridFullHeight = () => {
  const classes = useStyles()
  return (
    <div className={classes.root}>
    </div>
  )
}

export default GridFullHeight;
